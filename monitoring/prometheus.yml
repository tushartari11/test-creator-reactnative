global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "springboot-app"
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: [ "testcreator-app:8080" ]
# Add more jobs as needed for other services
